To run this program, 
requirements must be installed, 
the redis image must be run, 
celery must be run, 
the program must be run server, 
and then the api crawl must be called.
Addurl api must be used to add URL.
super user :{
    username:admin,
    password:admin
}